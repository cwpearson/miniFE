list(APPEND MINIFE_SRCS 
  ${CMAKE_CURRENT_LIST_DIR}/YAML_Doc.cpp
  ${CMAKE_CURRENT_LIST_DIR}/YAML_Element.cpp
)
set(MINIFE_SRCS ${MINIFE_SRCS} PARENT_SCOPE)

list(APPEND MINIFE_INCL_DIRS
  ${CMAKE_CURRENT_LIST_DIR}
)
set(MINIFE_INCL_DIRS ${MINIFE_INCL_DIRS} PARENT_SCOPE)
